/** @type {import('next').NextConfig} */
const nextConfig = {
    // This is a standard Next.js config.
    // No special output configuration is needed as Netlify will handle it.
};

module.exports = nextConfig;
